<div class="dashboard-container">
    <div class="dashboard-item">
        <div class="dashboard-item-title"><?= $block->escapeHtml(__('Total Batches')) ?></div>
        <div class="dashboard-item-value"><?= (int)$block->getTotalBatches() ?></div>
    </div>
    <div class="dashboard-item">
        <div class="dashboard-item-title"><?= $block->escapeHtml(__('Active Batches')) ?></div>
        <div class="dashboard-item-value"><?= (int)$block->getActiveBatches() ?></div>
    </div>
    <div class="dashboard-item">
        <div class="dashboard-item-title"><?= $block->escapeHtml(__('Expired Batches')) ?></div>
        <div class="dashboard-item-value"><?= (int)$block->getExpiredBatches() ?></div>
    </div>
    <div class="dashboard-item">
        <div class="dashboard-item-title"><?= $block->escapeHtml(__('Near Expiry (30 days)')) ?></div>
        <div class="dashboard-item-value"><?= (int)$block->getNearExpiryBatches() ?></div>
    </div>
</div>
<style>
    .dashboard-container { display: grid; grid-template-columns: repeat(4, 1fr); gap: 20px; margin: 20px 0; }
    .dashboard-item { background: #fff; border: 1px solid #e3e3e3; border-radius: 4px; padding: 20px; text-align: center; }
    .dashboard-item-title { font-size: 13px; color: #666; margin-bottom: 8px; }
    .dashboard-item-value { font-size: 32px; font-weight: 700; color: #333; }
</style>
